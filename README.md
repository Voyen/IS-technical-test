# InteractSport Technical Exercise

For local development this is run on Docker Compose.

First time build:

```sh
make build
```

Run the solution:

```sh
make run
```

Stop the solution:

```sh
make stop
```
